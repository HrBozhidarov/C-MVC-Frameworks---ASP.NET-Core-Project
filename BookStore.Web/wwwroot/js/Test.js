
    var defaultEditValue = "neshto";
    let selectedName = $('span.k-input').text();
    var a = 0;

            if (selectedName == null || selectedName == "") {
        selectedName = defaultEditValue;
    }
 